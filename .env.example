# Configuración de entorno para FACil

# Clave secreta de Django (genera una nueva para producción)
SECRET_KEY=tu_clave_secreta_aqui

# Modo debug (False en producción)
DEBUG=False

# Hosts permitidos (lista separada por comas)
ALLOWED_HOSTS=tu-dominio.com,www.tu-dominio.com

# URL de la base de datos (para Railway, se configura automáticamente)
DATABASE_URL=postgresql://usuario:password@host:puerto/dbname

# Otras configuraciones opcionales
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=tu-email@gmail.com
# EMAIL_HOST_PASSWORD=tu-password