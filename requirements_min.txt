# Requisitos mínimos detectados en el código
# Versión de Python: 3.13 (gestiona con tu entorno virtual)

Django==5.2.5

# Generación de PDFs (usado por vistas que convierten HTML a PDF)
xhtml2pdf>=0.2.17
reportlab>=4.0.4

# Procesamiento de imágenes (ImageField en modelos)
Pillow>=10.0.0

# Dependencias opcionales útiles en desarrollo / test (descomentar si las necesitas)
# pytest-django>=4.5.2
# factory-boy>=3.3.0

# Notas:
# - He dejado fuera paquetes que no aparecen en el código (ej. psycopg2-binary, django-environ,
#   django-crispy-forms, gunicorn, whitenoise). Añádelos si tu despliegue o entorno local los requiere.
# - Chart.js se utiliza desde CDN en las plantillas (no va aquí como dependencia pip).
